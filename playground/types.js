/*console.log('it works');

const sentence = 'she/s so cool'; 

const name = ' Nataliia ';
const middle = ' Nataly ';
const last = ' Kokidko ';

/*const hello = 'Hello my name is ' + name + ' Nice to meet you';
//let hello2 = 'Hello my name is ';
//hello2 = hello2 + name;
//hello2 = hello2 + 'Nice to meet you';

const hello = `Hello my name is ${name}. Nice to meet you. I am ${1 + 40} years old`;

const html = `
<div>
<h2>${name}</h2>
<p>${hello}</p>
</div>
`; 

document.body.innerHTML = html;

const age = 100.5;
const name = 'Nataly';
const a = 10;
const b = 20;

const smarties = 20;
const kids = 3;
const eachKidGets = Math.floor(smarties / kids);
console.log(`Each kids gets ${eachKidGets }`);

 const person = {
    first: 'Nataly',
    last: 'Kokidko',
    age: 41
 };

let dog;
 console.log(dog);
 dog = 'Margo';

 let somethingUndefined;
 const somethingNull = null;

 const cher = {
    first: 'Cher'
 };

 const teller = {
    first: 'Raymond',
    last: 'Teller'
 }

 teller.first = 'Teller';
 teller.last = null;*/

 let isDrawing = false;
 let age = 18;
 const ofAge = age > 19;
 console.log(ofAge);

 age = 100;
 let age2 = 100;
    